# generated build artifacts
nebula/target/

# local runtime values — the nebula binary, your networks and keys,
# per-instance properties. Never committed.
runtime/nebula/bin/
runtime/nebula/html/
runtime/nebula/networks/
runtime/nebula/botd.properties

# OS / IDE noise
.DS_Store
.idea/
out/

# builder-written lockfile for the generated crate
nebula/Cargo.lock
